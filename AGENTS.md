## GitHub pull requests

- Never prefix PR titles with `[codex]`.

## Development

- Use `npm run typecheck`, `npm test`, and `npm run build` before publishing changes.
- Keep the CLI generic; user- or company-specific paths belong in config examples, not core defaults.
